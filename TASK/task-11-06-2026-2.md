You are working inside the folder:

'/Users/zubarstanislav/Projects/Drafts/Aviation-quiz/ICAO Picture Commenting Trainer — Step 1'

Your task is to transform the existing folder into a complete educational learning material for practicing ICAO Aviation English picture description skills.

Main goal:

Create a full self-contained learning module that helps the student learn how to describe aviation pictures, especially Step 1: Topic Statement.

The material must teach the student to move from keywords to simple sentences, then to stronger exam-safe descriptions, and finally to independent speaking practice.

Current folder structure:

- PNG image files
- Markdown files with Step 1 scripts
- mp3 folder with audio files and SRT subtitles
- songs folder with memorization songs

Do not delete or rename the existing source files unless necessary.
Do not invent unrelated aviation theory.
Use the existing Markdown scripts as the main source of learning content.
Use the images, MP3 files, SRT files, and songs as learning assets.

Expected result:

Create a complete static educational website or learning package that can be opened locally in a browser.

Technology requirements:

Use only:

- HTML
- CSS
- Vanilla JavaScript

Do not use:

- React
- external frameworks
- CDN libraries
- build tools
- backend
- online dependencies

The result must work by opening an HTML file directly in the browser.

Create these files:

1. index.html
2. styles.css
3. script.js
4. README.md
5. optional: data.js or lessons.json if useful

Learning module structure:

The page must contain:

1. Course title

Example:
ICAO Picture Description Trainer — Step 1: Topic Statement

1. Short explanation of the skill

Explain that Step 1 is a short general overview of the image.
The student should learn to say:

- what the picture depicts;
- what kind of aviation situation it is;
- what aircraft or equipment are involved;
- whether the situation looks normal, abnormal, or dangerous.

1. Lesson cards for each picture

Create one lesson card for every available image/script pair:

- 01-runway-incursion
- 02-crosswind-landing
- 03-engine-ground-collision
- 05-highway-forced-landing
- 06-military-tanker-landing
- 07-gibraltar-apron
- 08-aircraft-in-water
- 09-aircraft-with-collapsed-nose-gear

Each lesson card must include:

- image preview
- lesson title
- key vocabulary
- simple version
- stronger version
- full exam version
- audio player for MP3
- subtitle or transcript section from the Markdown file
- song player if a relevant song exists
- speaking practice prompt

1. Learning flow for each lesson

Each lesson must guide the student through these stages:

Stage 1 — Look at the picture

Prompt:
Look at the image. Do not speak yet. Identify the main aviation situation.

Stage 2 — Learn key words

Show 5–8 key words or short phrases from the Markdown script.

Stage 3 — Listen and repeat

Add the MP3 audio player from the mp3 folder.

Stage 4 — Simple version

Show the simple description.
The student should repeat it aloud.

Stage 5 — Stronger version

Show the stronger ICAO Level 4–5 version.
The student should repeat it aloud.

Stage 6 — Full exam version

Show the full version.
The student should try to say it clearly without reading too much.

Stage 7 — Say it yourself

Show only keywords.
Hide the full answer.
The student must describe the picture from memory.

Stage 8 — Self-check

Show buttons:

- Show answer
- Hide answer
- Mark as practiced

1. Global study mode

Add a section called:

Daily Training Mode

It should explain a simple 20-minute routine:

- 5 minutes: listen to one audio script
- 5 minutes: repeat after audio
- 5 minutes: speak using only keywords
- 5 minutes: describe a random picture without reading the answer

1. Random practice mode

Add a button:

Random Picture Practice

When clicked, it should randomly select one picture and show:

- image
- keywords only
- timer for 60 seconds
- button to reveal exam-safe answer

The goal is to train real speaking, not passive reading.

1. Progress tracking

Use localStorage to track:

- practiced lessons
- completed lessons
- number of practice attempts
- last practiced date

Display simple progress:

Example:
You practiced 5 of 8 lessons.
Total attempts: 14.

1. Audio and subtitles

For each lesson, connect:

- the correct MP3 file from /mp3
- the correct SRT file from /mp3 if available

SRT subtitles may be shown as plain transcript text.
If parsing SRT is easy, parse it and show subtitles line by line.
If not, show the Markdown script transcript instead.

1. Songs section

Create a separate section:

Memorization Songs

Use the files in the songs folder.

Each song item should include:

- title from filename
- audio player
- short note: “Use this song for memorization, but use the TTS audio for exam-style speaking rhythm.”

1. Speaking templates section

Create a reusable template section.

Include these templates:

Template 1 — Normal operation

This picture depicts a normal airport operation.
I can see a commercial aircraft on the apron.
The situation looks normal and well controlled.

Template 2 — Abnormal or emergency situation

This picture depicts an abnormal aviation situation.
An aircraft appears to be involved in a serious incident.
The situation may be dangerous and requires attention from the crew or airport services.

Template 3 — Runway incident

This picture depicts a runway incident.
One aircraft appears to be on or near the runway.
This may create a safety risk if separation is not maintained.

Template 4 — Aftermath of an incident

This picture depicts the aftermath of an aviation incident.
The aircraft appears to have sustained visible damage.
Emergency personnel may be securing the area.

1. Design requirements

Make the interface:

- clean
- modern
- aviation-style
- easy to use during study
- readable on laptop and tablet
- mobile-friendly
- not overloaded

Use visual sections:

- image area
- audio area
- script area
- practice area
- progress area

Use clear buttons:

- Start lesson
- Play audio
- Show keywords
- Show simple version
- Show stronger version
- Show exam version
- Hide answer
- Mark as practiced
- Random practice

1. Content rules

Use only the existing folder files as the main source.

Do not create fake aviation incidents.
Do not add unsupported technical details.
You may simplify or reorganize the existing text for learning purposes.
Keep all English suitable for ICAO Level 4–5.
Ukrainian explanations may be added for study instructions, but aviation speaking answers must remain in English.

1. README.md

Create a README.md explaining:

- what this learning module is for
- how to open it
- folder structure
- how to add a new picture lesson
- naming convention for files
- how to add MP3 and SRT files
- how to add songs
- recommended daily training routine

1. Quality check

Before finishing, verify:

- index.html opens locally
- all image paths work
- all MP3 paths work
- all lesson cards are present
- random practice works
- progress tracking works
- no external dependencies are used
- design is responsive
- the student can use the page for real speaking practice

Important:

The final product is not just a gallery.
It must be a practical ICAO speaking trainer.

The student should be able to open the page, choose a picture, listen, repeat, hide the answer, and then describe the picture aloud using only keywords.

Додай ще це:

# ТЗ: Усний тренажер «Active Recall & Audio Drill»

## 1. Призначення та концепція

**Active-Recall / Spoken-Fluency тренажер** для автоматизації стійких словосполучень (chunks / collocations).

Замість механічного перекладу або зазубрювання ізольованих слів, користувач отримує контекстуальне визначення або комунікативну задачу англійською, самостійно формулює і **вимовляє цільову фразу вголос**, а потім миттєво перевіряє себе за допомогою тексту-еталону та інтегрованого аудіо-супроводу (TTS).

## 2. Архітектура даних (Data Schema)

Дані зберігаються у локальних JSON-файлах уроків. Тренажер активується лише за наявності масиву `drills` у структурі даних.

### Формат JSON (Поля об'єкта)

Кожен елемент масиву `drills` — це структурований об'єкт із трьома полями:

- `task` — дескрипція/визначення ситуації англійською мовою (без використання цільового слова).
- `target` — еталонна стійка фраза (collocation), яку потрібно згадати й вимовити.
- `audio_id` *(опціонально)* — ідентифікатор попередньо згенерованого TTS-файлу для озвучення еталону.

JSON

```
"drills": [
  {
    "task": "Use the professional medical phrase for performing chest compressions and rescue breaths.",
    "target": "administer CPR",
    "audio_id": "drill_01_cpr"
  },
  {
    "task": "Say that the flight continued its journey again after a temporary interruption or unscheduled stop.",
    "target": "flight resumed",
    "audio_id": "drill_01_resumed"
  }
]
```

## 3. Функціональні вимоги та UI/UX Логіка

### Стан додатка (State Management)

- `items`: масив об'єктів з JSON.
- `history`: масив індексів уже пройдених карток (для уникнення повторів).
- `currentIndex`: індекс поточної активної картки (старт: `-1`).
- `isRevealed`: булеве значення, що контролює видимість еталону (`false` за замовчуванням).

### Життєвий цикл та логіка взаємодії (Покроково)

1. **Стартовий стан:**
   - Поле картки показує інструкцію: *“Read the description, say the collocation out loud, then check yourself.”*
   - Кнопка **«Next Drill»** активна.
   - Кнопка **«Show Answer»** та кнопка аудіо-плеєра — `disabled`.
2. **Дія: Клік на «Next Drill»**
   - Генерується випадковий індекс, який **не дорівнює** поточному і якого **немає** в `history` (якщо довжина `history` < `items.length`). Якщо всі картки пройдені, `history` обнуляється.
   - Текст завдання (`task`) виводиться на екран.
   - Поле відповіді (`target`) повністю приховане.
   - `isRevealed` встановлюється в `false`.
   - Кнопка **«Show Answer»** стає активною.
3. **Дія: Клік на «Show Answer»**
   - Поле відповіді плавно відображається на екрані.
   - Кнопка **«Show Answer»** стає `disabled`.
   - **Авто-відтворення:** Якщо реалізовано TTS, додаток автоматично програє аудіофайл еталона (або генерує звук «на льоту» через OS-модуль мовлення). Користувач може повторно прослухати фразу за допомогою іконки «Play».

## 4. Макет інтерфейсу (UI Layout)

Дизайн має бути мінімалістичним, з високим рівнем контрасту, оптимізованим під швидку роботу «наосліп» з клавіатури.

```
+-------------------------------------------------------------+
|                        AUDIO DRILL                          |
|                     [ Card 3 of 14 ]                        |
+-------------------------------------------------------------+
|                                                             |
|  TASK:                                                      |
|  "Say that the medical professional officially advised      |
|   sending the patient to a hospital."                       |
|                                                             |
+-------------------------------------------------------------+
|                                                             |
|  TARGET (Hidden by default):                                |
|  » recommended hospitalization «   [Icon: Play Audio]       |
|                                                             |
+-------------------------------------------------------------+
|       [ Spacebar: Show Answer ]      [ Enter: Next Drill ]  |
+-------------------------------------------------------------+
```

### Гарячі клавіші (Обов'язково для Fluent-воркфлоу)

- `Space` — Показати відповідь (як в Anki).
- `Enter` / `Right Arrow` — Наступне завдання.
- `R` — Повторити аудіо-еталон.

## 5. Технічні інструкції для реалізації на Python (PySide6)

Для десктопного додатка логіка реалізується через зв'язку елементів керування у `QWidget`.

### Основні компоненти:

- `QLabel` для виведення тексту завдання (`task`) з увімкненим переносом рядків (`setWordWrap(True)`).
- Контейнер `QFrame` для блоку відповіді, стиль якого перемикає видимість (`setVisible(False)` $\rightarrow$ `setVisible(True)`).
- `QMediaPlayer` + `QAudioOutput` для програвання аудіо-файлів з папки рендерингу уроку.

### Сніппет ключової логіки перемикання станів (PySide6/Python):

Python

```
import random
from PySide6.QtWidgets import QWidget, QVBoxLayout, QLabel, QPushButton, QHBoxLayout
from PySide6.QtCore import Qt

class DrillWidget(QWidget):
    def __init__(self, drills_data):
        super().__init__()
        self.drills = drills_data  # Масив об'єктів з JSON
        self.history = []
        self.current_index = -1
        self.init_ui()

    def init_ui(self):
        self.layout = QVBoxLayout(self)
        
        # Елементи інтерфейсу
        self.lbl_task = QLabel("Click 'Next Drill' to start practicing.")
        self.lbl_task.setAlignment(Qt.AlignmentFlag.AlignCenter)
        self.lbl_task.setWordWrap(True)
        
        self.lbl_target = QLabel("")
        self.lbl_target.setAlignment(Qt.AlignmentFlag.AlignCenter)
        self.lbl_target.setStyleSheet("color: #2563eb; font-weight: bold; font-size: 18px;")
        self.lbl_target.hide() # Сховано на старті
        
        # Кнопки керування
        self.btn_show = QPushButton("Show Answer (Space)")
        self.btn_next = QPushButton("Next Drill (Enter)")
        self.btn_show.setEnabled(False)
        
        self.btn_show.clicked.connect(self.show_answer)
        self.btn_next.clicked.connect(self.next_drill)
        
        # Розміщення
        btn_layout = QHBoxLayout()
        btn_layout.addWidget(self.btn_show)
        btn_layout.addWidget(self.btn_next)
        
        self.layout.addWidget(self.lbl_task)
        self.layout.addWidget(self.lbl_target)
        self.layout.addLayout(btn_layout)

    def next_drill(self):
        if not self.drills:
            return
            
        if len(self.history) >= len(self.drills):
            self.history.clear() # Обнулення циклу
            
        available_indices = [i for i in range(len(self.drills)) if i not in self.history and i != self.current_index]
        
        # Крайній випадок, якщо елемент один
        if not available_indices:
            available_indices = [0]
            
        self.current_index = random.choice(available_indices)
        self.history.append(self.current_index)
        
        # Оновлення UI
        self.lbl_task.setText(self.drills[self.current_index]["task"])
        self.lbl_target.setText(self.drills[self.current_index]["target"])
        self.lbl_target.hide()
        
        self.btn_show.setEnabled(True)
        self.btn_next.setEnabled(False) # Змушуємо спочатку подивитися відповідь

    def show_answer(self):
        self.lbl_target.show()
        self.btn_show.setEnabled(False)
        self.btn_next.setEnabled(True)
        self.play_audio_ecc() # Виклик методу аудіо

    def play_audio_ecc(self):
        # Тут підключається логіка QMediaPlayer для відтворення 
        # файлу self.drills[self.current_index]["audio_id"]
        pass
```

## 6. Критерії готовності (Definition of Done)

1. **Повна ізоляція мови:** У тасках і відповідях немає українських літер. Тільки англійські описи концептів.
2. **Захист від дублів:** Алгоритм випадкового вибору не показує одну й ту саму картку двічі поспіль, доки не пройдено весь пул завдань.
3. **Keyboard-Driven:** Тренажер повністю проходить цикл «Опис $\rightarrow$ Промовляння $\rightarrow$ Перевірка $\rightarrow$ Наступний» за допомогою гарячих клавіш без використання миші.